// import * as constants from '../constants';

const initialState = {
  user: null,
  errors: null,
  loading: false,
  mfa_needed: false,
  authenticated: false
};

export default (state = initialState, action) => {
  // let x;
  switch (action.type) {
    default:
      return state;
  }
};
